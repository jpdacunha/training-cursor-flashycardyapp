---
alwaysApply: true
---
# shadcn/ui Usage Guidelines

This project uses **shadcn/ui** for all UI elements and components.

## Configuration

shadcn/ui is configured in [components.json](mdc:components.json) with the following settings:
- Style: New York
- Base Color: Neutral
- Icon Library: lucide-react
- Components Path: `@/components/ui`
- Utils Path: `@/lib/utils`

## Adding Components

To add new shadcn/ui components to the project, use:

```bash
npx shadcn@latest add <component-name>
```

Examples:
```bash
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add input
```

## Initialization (if needed)

If shadcn/ui is not initialized in the project, use:

```bash
npx shadcn@latest init -d
```

## STRICT USAGE POLICY

**CRITICAL RULE: ABSOLUTELY NO CUSTOM UI COMPONENTS ALLOWED**

This project **EXCLUSIVELY** uses shadcn/ui components for ALL UI elements. You must **NEVER** create custom UI components from scratch.

### DO ✅

- **ALWAYS** use shadcn/ui components for every UI element
- **ALWAYS** check if a shadcn/ui component exists before writing any UI code
- **ALWAYS** add missing shadcn/ui components using `npx shadcn@latest add <component-name>`
- **ALWAYS** use the `cn()` utility from [lib/utils.ts](mdc:lib/utils.ts) for conditional class merging
- **ALWAYS** use lucide-react icons for all iconography needs
- **ALWAYS** leverage Tailwind CSS for styling customizations of shadcn/ui components

### DON'T ❌

- **NEVER** create custom UI components (buttons, inputs, cards, modals, etc.)
- **NEVER** write custom styled divs when a shadcn/ui component exists
- **NEVER** build forms without using shadcn/ui form components
- **NEVER** create custom dialogs, modals, or overlays - use shadcn/ui Dialog, Sheet, or Popover
- **NEVER** bypass shadcn/ui by creating "simple" custom components

## Clerk Integration Requirements

### Authentication UI Components

When integrating Clerk authentication, you **MUST** follow these rules:

1. **Sign In/Sign Up Buttons**: Use shadcn/ui Button components
   ```typescript
   import { Button } from "@/components/ui/button";
   
   // ✅ CORRECT
   <Button onClick={() => openSignInModal()}>Sign In</Button>
   <Button onClick={() => openSignUpModal()}>Sign Up</Button>
   ```

2. **Authentication Modals**: Use shadcn/ui Dialog component for sign-in and sign-up
   ```typescript
   import { Dialog, DialogContent, DialogTrigger } from "@/components/ui/dialog";
   import { SignIn, SignUp } from "@clerk/nextjs";
   
   // ✅ CORRECT: Clerk forms in shadcn/ui Dialog
   <Dialog>
     <DialogTrigger asChild>
       <Button>Sign In</Button>
     </DialogTrigger>
     <DialogContent>
       <SignIn />
     </DialogContent>
   </Dialog>
   ```

3. **DO NOT** use Clerk's default UI components standalone:
   ```typescript
   // ❌ WRONG: Clerk components without shadcn/ui wrapper
   <SignInButton mode="modal" />
   
   // ✅ CORRECT: shadcn/ui Button with Clerk Dialog
   <Dialog>
     <DialogTrigger asChild>
       <Button>Sign In</Button>
     </DialogTrigger>
     <DialogContent>
       <SignIn />
     </DialogContent>
   </Dialog>
   ```

4. **User Profile/Settings**: Wrap in shadcn/ui Dialog or Sheet
   ```typescript
   // ✅ CORRECT
   <Sheet>
     <SheetTrigger asChild>
       <Button>Profile</Button>
     </SheetTrigger>
     <SheetContent>
       <UserProfile />
     </SheetContent>
   </Sheet>
   ```

## Available Component Categories

- **Form elements**: Input, Select, Checkbox, Radio, Switch, Textarea, Label, Form
- **Layout components**: Card, Sheet, Dialog, Tabs, Separator, Accordion
- **Navigation**: Navigation Menu, Breadcrumb, Pagination, Menubar
- **Feedback**: Alert, Toast, Progress, Skeleton, Spinner
- **Data display**: Table, Badge, Avatar, Calendar, Command
- **Overlays**: Dialog, Popover, Tooltip, Sheet, Drawer, Modal
- **Interactive**: Button, Dropdown Menu, Context Menu, Hover Card

## Before Writing Any UI Code

**MANDATORY CHECKLIST:**

1. [ ] Search shadcn/ui documentation for existing component
2. [ ] If component exists, add it using `npx shadcn@latest add <component-name>`
3. [ ] If multiple components could work, choose the most appropriate shadcn/ui component
4. [ ] ONLY use Tailwind CSS for styling adjustments to shadcn/ui components
5. [ ] Never create custom components - always use or compose shadcn/ui components

## Reference

Always check the [shadcn/ui documentation](https://ui.shadcn.com/docs/components) for the latest components and usage patterns.

**Remember**: If you find yourself creating a custom UI component, STOP and find the equivalent shadcn/ui component instead.
